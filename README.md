# 🔄 Swap Nodes in Pairs (LeetCode 24)

## 📌 Problem
Given a linked list, swap every two adjacent nodes and return its head.

⚠️ You must solve the problem **without modifying node values**, only changing pointers.

---

## 🧾 Example

**Input:**
